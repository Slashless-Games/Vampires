extends Node

signal enemy_hit;
signal enemy_consumed(amount);

signal player_hit;
signal player_attacks;


signal distance_left(player_distance, player_distance_away);

#TODO: For more areas...
signal new_area;
